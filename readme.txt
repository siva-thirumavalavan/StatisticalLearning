Q1_Boston and Q2_Census are our main codes
The user does not have to specify a working directory. The code will run out of the box (No need to install any packages)


The python code was our experiment on Neural Net. We have included that since we have mentioned the code in our report. To run the python code user will have to install the libraries specified in the code.